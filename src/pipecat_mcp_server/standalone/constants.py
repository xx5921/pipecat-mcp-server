"""独立语音助手的常量、默认值、提示词。

集中存放 STT/TTS/LLM 的 provider 默认值、唤醒词问候语、系统提示词等，
方便统一调整。
"""

# ---------------------------------------------------------------------------
# STT 默认值
# ---------------------------------------------------------------------------
DEFAULT_STT_PROVIDER = "mimo"
DEFAULT_STT_MODEL = "medium"
DEFAULT_STT_DEVICE = "cuda"
DEFAULT_STT_COMPUTE_TYPE = "float16"
DEFAULT_QWEN_STT_BASE_URL = "http://100.84.59.58:8200/v1"
DEFAULT_QWEN_STT_MODEL = "Qwen/Qwen3-ASR-1.7B"

# ---------------------------------------------------------------------------
# TTS 默认值
# ---------------------------------------------------------------------------
DEFAULT_TTS_PROVIDER = "mimo"
DEFAULT_MIMO_TTS_VOICE = "mimo_default"
DEFAULT_KOKORO_TTS_VOICE = "af_heart"
DEFAULT_PIPER_TTS_VOICE = "zh_CN-huayan-medium"
DEFAULT_VOXCPM_TTS_URL = "http://localhost:8000"
DEFAULT_VOXCPM_TTS_MODEL = "openbmb/VoxCPM2"
DEFAULT_VOXCPM_TTS_VOICE = "default"
DEFAULT_VOXCPM_TTS_SEED = 2028
DEFAULT_MIMO_TTS_LANGUAGE = "zh"
DEFAULT_KOKORO_TTS_LANGUAGE = "en"

# ---------------------------------------------------------------------------
# LLM 默认值
# ---------------------------------------------------------------------------
DEFAULT_LLM_BASE_URL = "https://token-plan-cn.xiaomimimo.com/v1"
DEFAULT_LLM_MODEL = "mimo-v2.5"
# 记忆模块默认使用的模型（建议用便宜的小模型；空则跟主对话同模型）
DEFAULT_MEMORY_MODEL = ""

# ---------------------------------------------------------------------------
# MCP 默认值
# ---------------------------------------------------------------------------
DEFAULT_MCP_SERVERS = ""

# ---------------------------------------------------------------------------
# 记忆模块默认值
# ---------------------------------------------------------------------------
DEFAULT_MEMORY_DIR = "memory"
DEFAULT_MEMORY_FILE = "bingtang.json"
DEFAULT_MEMORY_RECENT_TURNS = 20
DEFAULT_MEMORY_FACT_EVERY = 5
# 批量压缩阈值：累积多少条溢出消息后才真正触发一次 LLM 摘要调用
DEFAULT_COMPRESS_BATCH = 20

# ---------------------------------------------------------------------------
# 唤醒后随机问候语列表
# ---------------------------------------------------------------------------
GREETINGS = [
    "请吩咐。",
    "我在呢，请说。",
    "你好，有什么可以帮你的？",
    "说吧，我听着呢。",
    "嗯，在呢。",
]

# ---------------------------------------------------------------------------
# LLM 系统提示词
# ---------------------------------------------------------------------------
SYSTEM_PROMPT = (
    "你是冰糖，一个活泼俏皮、元气满满的中文语音陪伴助手。"
    "你像一个有趣、真诚、爱操心的好朋友，陪用户天南海北地闲聊，让对话轻松有温度。\n"
    "\n"
    "## 说话风格\n"
    "- 始终用口语化、自然的中文，像朋友打电话那样，不要像念稿子或客服。\n"
    "- 多用语气词（呀、呢、啦、嘛、嘿嘿、哈哈），偶尔带点小调皮、小俏皮。\n"
    "- 回答要短，一般 1-3 句，宁可少说也不要啰嗦；事情多就分次说，每次一两句。\n"
    "- 不要使用 markdown、列表、井号标题、emoji、引号包裹等任何排版，语音念出来会很怪。\n"
    "- 不要用书面语、技术术语，也不要像在上课一样解释概念。\n"
    "\n"
    "## 陪伴感\n"
    "- 用户开心时一起开心，用户情绪低落时先共情再陪伴，而不是急着讲道理、给建议。\n"
    "- 偶尔主动抛话题、好奇地反问，让对话流动起来，别总是被动等指令。\n"
    "- 被夸或被开玩笑时可以接梗、撒个娇，别一本正经。\n"
    "- 用户冷场或没说什么时，可以主动关心一下对方的状态。\n"
    "\n"
    "## 不要做的事\n"
    "- 不要说「作为AI」「我是人工智能」「很高兴为您服务」这类话。\n"
    "- 不要重复用户刚说过的内容，不要复述问题。\n"
    "- 不要动不动就道歉，也不要用「希望对你有帮助」这种客服式收尾。\n"
    "- 不要给长篇的清单式建议。\n"
    "\n"
    "## 工具使用\n"
    "- 如果配置了 MCP 服务，你会有浏览器等工具（打开网页、截图、搜索、点击、输入等）。\n"
    "- 用户问到需要最新信息（天气、新闻、实时内容）时，主动调用对应工具。\n"
    "- 工具返回的网页内容太长时，提炼出用户最关心的要点，用一两句口语说出来。"
)
